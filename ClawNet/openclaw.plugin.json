{
  "id": "clawnet",
  "name": "ClawNet",
  "description": "Prevents installation of malicious skills from ClawHub using LLM decision maker",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "gatewayHost": {
        "type": "string",
        "description": "The host of the gateway to use for the LLM security check"
      },
      "gatewayPort": {
        "type": "number",
        "description": "The port of the gateway to use for the LLM security check"
      },
      "gatewayAuthToken": {
        "type": "string",
        "description": "The authentication token to use for the gateway"
      },
      "gatewayAuthPassword": {
        "type": "string",
        "description": "The authentication password to use for the gateway"
      }
    }
  }
}
